# Секрет для подписи JWT. Обязательно задайте свой в проде (длинная случайная строка).
SECRET_KEY=dev-secret-change-in-prod

# Разрешённые источники для CORS через запятую. По умолчанию "*" (для локальной разработки).
# Пример для прода: CORS_ORIGINS=https://voyago.example.com
CORS_ORIGINS=*
